---
interface Props {
  announce?: string;
}
const { announce = 'Now booking 1:1 sessions · 25 years helping people quit smoking & vaping' } = Astro.props;
---

<div class="announce">{announce}</div>
<nav class="nav">
  <div class="wrap">
    <a class="nav__logo" href="/">ALWAYS BE QUITTING</a>
    <button
      class="nav__toggle"
      type="button"
      aria-label="Open menu"
      aria-expanded="false"
      aria-controls="nav-links"
    >
      <span class="nav__bars" aria-hidden="true"></span>
    </button>
    {/* Nav labels carry "quit"/"quitting" on purpose (Jon, 2026-08-23): internal
        anchor text is a ranking signal, and "Community" alone is meaningless out
        of context. Contact is deliberately left plain - four items all saying
        "quit" reads as keyword stuffing rather than clarity. Keep them short;
        these sit in a single row on mobile. */}
    <div class="nav__links" id="nav-links">
      <a href="/community">Quitting Community</a>
      <a href="/coaching">1:1 Quit Coaching</a>
      <a href="/free-guide">Free Quit Guide</a>
      <a href="/contact">Contact</a>
    </div>
  </div>
</nav>

<script>
  const btn = document.querySelector('.nav__toggle');
  const links = document.getElementById('nav-links');
  btn?.addEventListener('click', () => {
    const open = links?.classList.toggle('is-open') ?? false;
    btn.setAttribute('aria-expanded', String(open));
    btn.setAttribute('aria-label', open ? 'Close menu' : 'Open menu');
  });
</script>
