# build output
dist/
.vercel/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*

# environment
.env
.env.*
!.env.example

# os
.DS_Store

# video masters - NEVER commit these. Large binaries live in git history
# permanently and cannot be removed without rewriting every commit.
# Drop source files in video-source/; only the encoded web file ships.
video-source/
image-source/
